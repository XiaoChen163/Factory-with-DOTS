## 自我介绍

untiy经历 
年初gamejam
大模型接入unity，MCP实现交互
利用unity dots ECS架构开发工厂游戏

参与南京仙地公司实习，开发线上项目前端界面功能

## 工厂游戏

### DOTS原理：
传统对象OOP内存碎片化、CPU缓存失效
ECS采取SOA连续内存分配

Entity 轻量化64位ID
Component 纯数据结构体
Archetype 具有完全相同组件组合的实体集合
Chunk Archetype拆分为chunk，固定大小内存块

JOB系统调度
Job系统以**chunk**为单位
将待处理数据划分为**多个批次**，分配给不同的工作线程执行

Safety/依赖解析
通过JobHandle自动构建依赖树，跟踪数据读写关系；
两个job无交叉条件就安排并行，有读写冲突则安排串行，避免竞争

Brust编译加速
job内部仅允许使用native容器和非托管对象，规避GC

### 60Tick固定步长模拟
FixedStepSimulationSystemGroup在世界初始化时设置Timestep为1f/60f
此后所有逻辑有关的系统统一标识[UpdateInGroup(typeof(FixedStepSimulationSystemGroup))]
再通过UpdateBefore/UpdateAfter形成流水线

### 双缓冲快照
物品传输的仲裁job负责，tick开始时加载所有传送带/分流器/合并器
的当前物品/进度/目标 复制进DynamicNodes，之后谁会赢谁能动就只改这份快照，最后提交后才一次性写回实体组件

### 链式解析
如果来源的目标格为空：直接允许；
如果目标格有物品：目标必须本身 Ready、有目标，并且它必须是它自己下游目标的赢家；然后继续检查目标的下游；
如果一路追到空位：整条链允许；
如果某个目标被占且它不能走、不 Ready、或没被选为下游赢家：整条链拒绝；
如果追到当前正在解析的节点，说明这是一个闭环：允许整环原子旋转；

### 原子提交
第一轮遍历把所有 已经接受的物品 来源的物品先存到 临时数组，同时把来源清空、进度置零；
第二轮遍历才把物品写进目标格，并把目标 进度置零

### 突发编译 + 并行执行
推进传送带上物品进度
加工建筑的出入口的接受与发送
建筑出入口的双缓冲交换

以上系统均是并行的，主线程只做调度和ECB挂接

### 近似O(N)仲裁算法
建筑和传送带的输出请求处理的节点只遍历一次，为每个准备好到下一个节点的物品设置唯一输出目标

### 拓扑缓存
仅在传送带结构有变化时重建模拟网络中的拓扑图，重建时复用NativeList的容量，不会分配新的内存
网格拓扑重建系统运行在传送带转移系统之前，因此不会发生脏数据或丢失

### CSV自动数据/资源管线
用csv配置所有物品、配方以及建筑的数据及它们的纯表现prefab的文件名
用文件名规范化匹配预制体
导入脚本读取这些csv表格并校验文件存在和参数合法性，当csv文件修改保存后自动将所有csv数据表保存为unity内数据资产，避免运行时读取
prefab仅有网格渲染/材质等表现组件，运行时通过blob创建相应Entity并为其写入出入端口、建筑适用配方等
物品prefab的图标由渲染器自动烘焙为png并导入为单张sprite

## unity Agent
不同于大模型操作编辑器，大模型可直接操纵运行时的游戏世界
大模型不仅可作为对话机器人，也能作为游戏世界真正交互的智能agent
这些内容都通过规范的A2A和MCP协议分层实现，并且能支持多NPC，多玩家，并且能够快速集成到现有项目

### A2A
agent之间的相互操作协议，通过json-rpc做请求响应，用SSE做流式输出
A2A协议提供了 发送接受消息、执行/取消任务功能
在项目内主要维护NPC与玩家间的会话任务

完整流程：
1. unity通过message/stream发送玩家文本，附带实例、玩家、NPC的id
2. go端A2A server创建和校验会话，发给大模型
3. 如果大模型只说话不传出工具调用，直接生成回复，A2A端流式传回unity给玩家
4. 如果返回工具调用，则下单给工具层
5. 工具层通过MCP协议发现unity中声明的工具，再由运行时Websocket通知unity执行对应操作Runtime Bridge
6. unity收到命令后塞入线程安全队列，主线程中通过update安全消费，消费完成后通知go端
7. go端再将结果返回给大模型，大模型再生成回复
8. 最终结果通过A2A流式传输回unity

### unity中SDK打包
SDK中主要打包了实体契约、工具契约、运行时和传输契约以及对话响应事件等的格式；定义了它们的接口和数据结构；

具体的网络协议、调度和游戏业务的实现都交给使用方进行实现，以保障开发环境和网络协议的弱依赖，减少开发者接入这套MCP对话工具的成本

附带使用示例，提供了NPC工具示例



